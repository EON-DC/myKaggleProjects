## A. 로딩·기본 구조 파악

1. Iris 데이터를 DataFrame으로 로드하고, 컬럼명을 `sepal_length, sepal_width, petal_length, petal_width, species`로 맞추세요.
2. `df.shape`, `df.head(3)`, `df.tail(3)`, `df.info()` 결과를 요약해 보세요.
3. 각 컬럼의 결측치 개수를 계산하세요.
4. `species`의 클래스 개수와 각 클래스 샘플 수를 구하세요(빈도표).

---

## B. 단변량 기술통계(Overall)

5. 연속형 4개 컬럼에 대해 `describe()`를 실행하고, 다음을 해석해 보세요:

   * 평균 vs 중앙값 차이가 큰 컬럼이 있는지
   * 표준편차가 가장 큰 컬럼은 무엇인지
6. 각 연속형 컬럼의 **분산(variance)**, **표준편차(std)**, **범위(range=max-min)** 를 한 번에 표로 만드세요.
7. 각 연속형 컬럼의 **사분위수(Q1, Q2, Q3)**와 **IQR(Q3-Q1)**을 계산해 표로 정리하세요.
8. 각 컬럼의 **왜도(skewness)**, **첨도(kurtosis)**를 계산하고, 값이 의미하는 바를 짧게 적어보세요.

---

## C. 그룹별 기술통계(GroupBy)

9. `species`별로 4개 연속형 컬럼의 평균을 구하고, 어떤 특성이 품종 구분에 유용해 보이는지 근거를 대세요.
10. `species`별로 **중앙값(median)**, **표준편차(std)**를 동시에 구해 MultiIndex 컬럼 형태로 만들고, 보기 좋게 정렬하세요.
11. `species`별로 `petal_length`의 **최댓값**과 **최솟값**을 구하고, 품종 간 겹침(overlap)이 있는지 확인하세요.
12. `species`별로 `petal_length`의 **IQR**을 계산해, 변동성이 가장 큰 품종을 찾으세요.

---

## D. 조건 필터링·정렬·인덱싱

13. `setosa`만 필터링한 뒤, `sepal_length` 내림차순 상위 5개 행을 출력하세요.
14. “꽃잎(petal)이 큰” 샘플을 정의해 보세요. 예: `petal_length >= 5.0 AND petal_width >= 1.8`

* 해당 조건을 만족하는 샘플 수, species 분포를 구하세요.

15. `versicolor` 중 `sepal_width`가 전체 상위 10%에 속하는 행만 선택하세요(quantile 사용).
16. `species`별 `petal_length` 평균이 큰 순서로 species를 정렬해 출력하세요.

---

## E. 상관·공분산·관계 요약

17. 4개 연속형 컬럼의 **상관행렬(corr)**을 만들고, 절댓값 기준 상위 2개 상관쌍을 찾으세요.
18. `species`별로 상관행렬을 각각 구해, 전체 상관과 달라지는 점을 비교하세요.
19. 공분산 행렬(cov)을 구하고, 상관행렬과의 차이를 설명해 보세요(스케일 관점).

---

## F. 이상치(outlier)·분포 점검

20. IQR 기준(1.5*IQR)으로 각 연속형 컬럼의 이상치 개수를 계산하세요. 컬럼별로 가장 많은 이상치를 가진 컬럼은?
21. `species`별로 `sepal_width` 박스플롯을 그렸다고 가정하고, 어떤 품종에서 이상치가 많을지 예측해 보세요(수치 근거 포함).
22. Z-score(표준화) 기준 |z|>3 이상치 개수를 구하고, IQR 방식과 결과가 다른 이유를 설명해 보세요.

---

## G. 요약표·피벗·다중 집계

23. `pd.pivot_table`로 `species`를 index로 하고, 값은 4개 연속형 컬럼의 평균/표준편차를 동시에 담는 표를 만드세요.
24. `petal_length`를 3개 구간(예: tertile)으로 나눠(binning, `pd.qcut`) 구간별 species 빈도표를 만드세요.
25. 위 빈도표를 “행=species, 열=구간, 값=개수” 형태로 재구성하세요(unstack).

---

## H. 간단한 통계적 해석(기술통계 기반)

26. “품종을 가장 잘 구분할 것 같은 단일 변수 1개”를 선택하고, **그 근거를 기술통계 지표(평균 차이, 분산, 겹침 여부 등)**로만 설명하세요.
27. `setosa`와 `versicolor`의 `petal_length` 평균 차이를 **효과크기(Cohen’s d)**로 계산해 보세요. (공식은 직접 찾아 쓰거나, d를 계산하는 함수를 만들어도 됩니다.)
28. `petal_length`에 대해 `species`별 95% 신뢰구간(평균)을 계산해 비교하세요(정규근사 또는 t-분포 중 선택).

---

## I. 미니 프로젝트(종합)

29. “한 장짜리 EDA 요약”을 만든다고 가정하고, 다음을 모두 포함하는 테이블/출력을 구성하세요.

* 데이터 품질(결측치/중복)
* 전체 기술통계 요약(핵심 5개 지표)
* species별 평균/표준편차 요약
* 상관 상위 2쌍
* 이상치 요약(IQR)

30. 위 요약을 바탕으로 “다음 분석 단계(예: 간단 분류모델)”로 넘어가려면 어떤 전처리/피처 선택을 하겠는지 5줄 이내로 제안하세요.

---
아래는 Iris 데이터셋 기준으로 **시각화 연습 문제(V1~V10)**를 추가한 것입니다. 

---

## V1. species 분포 막대그래프

**문제:** `species`별 샘플 개수를 막대그래프로 그리세요.

---

## V2. 연속형 4개 특성 히스토그램(2x2)

**문제:** `feature_cols` 4개에 대해 히스토그램을 2x2로 배치해 그리세요.

---

## V3. species별 박스플롯(특성 1개)

**문제:** `petal_length`를 `species`별 박스플롯으로 그리세요.

---

## V4. species별 박스플롯(특성 4개를 한 번에)

**문제:** 4개 연속형 특성 각각에 대해 `species`별 박스플롯을 2x2로 그리세요.

---

## V5. species별 바이올린 플롯(특성 1개)

**문제:** `petal_width`를 `species`별 바이올린 플롯으로 표현하세요.

---

## V6. 산점도: sepal_length vs sepal_width (species로 색 구분)

**문제:** `sepal_length`-`sepal_width` 산점도를 그리고, `species`에 따라 색이 달라지도록 하세요.

---

## V7. 산점도: petal_length vs petal_width + 클래스별 평균점 표시

**문제:** `petal_length`-`petal_width` 산점도를 그리고, 각 species의 평균점(centroid)을 X로 표시하세요.

---

## V8. 산점도 행렬(Scatter Matrix)

**문제:** 4개 연속형 특성에 대해 산점도 행렬(Scatter Matrix)을 그리세요. (대각선은 히스토그램)

---

## V9. 상관행렬 히트맵(Heatmap) — matplotlib만 사용

**문제:** `feature_cols`의 상관행렬을 히트맵으로 시각화하고, 축에 컬럼명을 표시하세요.

---

## V10. species별 평균±표준편차 에러바(예: petal_length)

**문제:** `species`별 `petal_length`의 평균과 표준편차를 막대(또는 점)+에러바로 표현하세요.

---
