## 1) 학습 방향 로드맵 (Pandas 숙련 목표)

1. **데이터 이해 & 검증(EDA 초반)**
    
    - shape, dtypes, 결측치 분포, 중복/유니크, 범주 분포 확인
        
    - “이 컬럼은 무엇을 의미하는가?”를 코드로 확인하는 습관
        
2. **정제/전처리(실무 핵심)**
    
    - 결측치 처리(단순 대체/그룹 기반 대체), 이상치 탐지
        
    - 문자열 파싱(Name/Title/Cabin), 카테고리 인코딩, 파생 변수
        
3. **집계 & 비교 분석**
    
    - groupby/agg, pivot_table, crosstab
        
    - “생존률” 같은 비율 지표를 안정적으로 계산하는 법(분모, NaN, 타입)
        
4. **조인 & 데이터 모델링**
    
    - train/test 분리 상태에서 동일 변환 적용
        
    - 외부 테이블(예: Deck lookup, Title mapping)을 merge/join으로 결합
        
5. **재현 가능성 & 파이프라인화**
    
    - 함수화, 체인 방식, 중간 산출물 검증(assert)
        
    - 성능(메모리/속도) 감각: astype('category'), 벡터화 vs apply
        

---

## 2) 문제 세트 (답안은 추후 요청 시 제공)

아래 문제는 “train.csv” 기준을 기본으로 하되, 일부는 “test.csv”도 함께 쓰도록 구성했습니다.  
각 문제의 **목표는 ‘정답 숫자’가 아니라, Pandas 조작을 자연스럽게 하도록 만드는 것**입니다.

---

# A. 로딩/점검/기초 EDA (기본 체력)

### A1. 데이터 로딩 및 스키마 리포트

- **문제:** `train.csv`를 읽고, (1) 행/열 수, (2) 각 컬럼 dtype, (3) 결측치 개수/비율을 한 번에 요약한 표를 만드세요.
    
- **무엇을 묻는가:** `read_csv`, `shape`, `dtypes`, `isna().sum()`, 비율 계산, `DataFrame` 구성 능력.
    

### A2. 유니크/중복 점검

- **문제:** PassengerId가 유니크인지 확인하고, 중복이 있다면 중복 행을 반환하세요. 또한 Name 중복 빈도 상위 10개를 뽑으세요.
    
- **무엇을 묻는가:** `duplicated`, `value_counts`, 인덱싱/필터링.
    

### A3. 범주형 분포 빠르게 보기

- **문제:** `Sex`, `Embarked`, `Pclass`의 분포를 “개수 + 비율”로 동시에 보여주는 요약 테이블을 각각 생성하세요.
    
- **무엇을 묻는가:** `value_counts(normalize=...)`, `concat`, 멀티 컬럼 요약 패턴.
    

---

# B. 결측치/이상치 처리 (실무의 70%)

### B1. 결측치 지도 만들기

- **문제:** 컬럼별 결측치 비율을 내림차순 정렬하여 상위 5개 컬럼을 구하고, 결측치 비율이 0인 컬럼 목록도 따로 구하세요.
    
- **무엇을 묻는가:** 결측치 진단, 정렬, 조건 필터링.
    

### B2. Age 결측치 “그룹 기반”으로 채우기

- **문제:** Age 결측치를 `Pclass`와 `Sex` 그룹의 중앙값으로 채우세요. 채우기 전/후 Age의 결측치 개수와 기술통계를 비교하세요.
    
- **무엇을 묻는가:** `groupby().transform('median')` 패턴, `fillna`, 전후 비교.
    

### B3. Fare 이상치 탐지 및 처리 전략 제안

- **문제:** Fare의 상위 1%를 이상치 후보로 보고, 해당 행을 추출하세요. 이후 (1) 그대로 둔다, (2) 로그 변환, (3) winsorize(상한 절단) 중 한 가지로 처리한 새 컬럼을 만드세요.
    
- **무엇을 묻는가:** 분위수 계산, 조건 필터, 파생 컬럼 생성, 변환 설계 감각.
    

---

# C. 문자열 파싱/파생 변수 (Pandas “손맛” 구간)

### C1. 이름에서 Title 추출

- **문제:** `Name`에서 호칭(Title: Mr, Mrs, Miss, Master 등)을 추출하여 `Title` 컬럼을 만들고, 희귀 타이틀을 “Rare”로 묶은 `Title2`를 만드세요(희귀 기준: 빈도 < 10).
    
- **무엇을 묻는가:** `str.extract`, 정규표현식, `value_counts`, 조건 치환.
    

### C2. Cabin에서 Deck 추출

- **문제:** `Cabin`의 첫 글자를 Deck(A~G 등)으로 보고 `Deck` 컬럼을 만드세요. Cabin 결측치는 `Unknown`으로 처리하세요.
    
- **무엇을 묻는가:** `fillna`, `str[0]`, 문자열 처리, 카테고리화.
    

### C3. Ticket의 접두어/숫자 분리

- **문제:** Ticket에서 알파벳/기호 접두어(있다면)와 숫자 부분을 분리해 `TicketPrefix`, `TicketNumber` 컬럼을 만드세요. 접두어가 없으면 `None` 처리.
    
- **무엇을 묻는가:** 문자열 정제, 정규표현식, `astype`/결측 처리.
    

---

# D. Groupby/Aggregation/Pivot (분석의 중심)

### D1. 생존률 테이블 3종 세트

- **문제:** 생존률(=Survived 평균)을 (1) Sex별, (2) Pclass별, (3) Sex×Pclass 2차원으로 각각 구하세요.
    
- **무엇을 묻는가:** `groupby().mean()`, `pivot_table`, 다차원 집계.
    

### D2. 연령대(bin)별 생존률

- **문제:** Age를 구간화하여(예: 0–9, 10–19, …, 70+) `AgeBand`를 만들고, `AgeBand`별 생존률과 인원수를 같이 표시하세요.
    
- **무엇을 묻는가:** `pd.cut`, 집계에서 count와 mean 동시 산출(`agg`).
    

### D3. 가족 단위 변수가 생존에 미치는 영향

- **문제:** `FamilySize = SibSp + Parch + 1`을 만들고, FamilySize별 생존률을 계산하세요. 추가로 `IsAlone(FamilySize==1)`를 만들고, IsAlone별 생존률을 계산하세요.
    
- **무엇을 묻는가:** 파생 변수, 불리언 컬럼, 집계 비교.
    

---

# E. Merge/Join & “train/test 일관성” (모델링 준비)

### E1. train/test 동시 전처리 프레임 만들기

- **문제:** `train.csv`와 `test.csv`를 각각 읽고, 동일한 전처리(예: Title, Deck, FamilySize)를 “같은 함수”로 적용한 뒤, 두 데이터의 컬럼 스키마가 일치하는지 검사하세요.
    
- **무엇을 묻는가:** 함수화, 재사용, `assign`, 스키마 검증.
    

### E2. 외부 매핑 테이블과 merge

- **문제:** `Title2`를 수치 그룹(예: {Mr:0, Mrs:1, Miss:2, Master:3, Rare:4})으로 바꾸는 매핑 테이블(DataFrame)을 만들고, 이를 merge로 결합하여 `TitleCode`를 생성하세요. (딕셔너리 map 금지)
    
- **무엇을 묻는가:** `merge` 기반 인코딩, 조인 키 설계.
    

### E3. Embarked 결측치 최빈값 대체를 “그룹 조건”으로

- **문제:** Embarked 결측치를 전체 최빈값이 아니라, `Pclass`별 최빈값으로 채우세요. 채우기 전/후 분포가 어떻게 바뀌는지 확인하세요.
    
- **무엇을 묻는가:** 그룹 기반 최빈값 추정, transform 응용, 분포 비교.
    

---

# F. 성능/품질/검증 (실전 감각)

### F1. dtype 최적화

- **문제:** `Sex`, `Embarked`, `Title2`, `Deck` 등을 category로 바꾸고, 메모리 사용량이 얼마나 줄었는지 전/후 비교 리포트를 만드세요.
    
- **무엇을 묻는가:** `astype('category')`, `memory_usage(deep=True)`.
    

### F2. 전처리 품질 체크(Assertion)

- **문제:** 전처리 후 다음 조건을 만족하는지 assert로 검증하세요.
    
    1. Age 결측치 0개
        
    2. Embarked 결측치 0개
        
    3. Fare는 음수 없음
        
    4. Deck는 {'A','B','C','D','E','F','G','T','Unknown'} 외 값 없음(있는지 검사)
        
- **무엇을 묻는가:** 데이터 품질 게이트(검증), `assert`, 집합 비교.
    

### F3. “apply를 벡터화로 바꾸기” 리팩토링

- **문제:** 임의로 apply로 작성한 Title 추출/FamilySize 계산을 벡터화 연산으로 바꿔서, 코드 라인 수와 실행 시간을 비교하세요(간단히 `%%timeit` 또는 time 모듈 사용).
    
- **무엇을 묻는가:** 벡터화 사고, 성능 측정, 리팩토링.
    

---

## 3) 학습 운영 방식(추천)

- 하루 목표 예시(30~60분):
    
    1. A/B 중 2문제
        
    2. C/D 중 1문제
        
    3. F 중 1문제로 마무리(검증/최적화)
        
- 각 문제를 풀 때 “정답”보다 중요한 체크리스트:
    
    - **중간 산출물 head( )로 확인**
        
    - 결측/타입/분포가 “의도대로” 바뀌었는지 **전후 비교**
        
    - 같은 작업을 train/test에 **일관되게** 적용할 수 있는지
        

---

